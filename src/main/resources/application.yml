server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/wakapix?characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: root
    password: Gzqlb2023618
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      idle-timeout: 300000
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
  mybatis-plus:
    configuration:
      map-underscore-to-camel-case: true
      log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    global-config:
      db-config:
        id-type: auto
        logic-delete-field: deleted
        logic-delete-value: 1
        logic-not-delete-value: 0
    mapper-locations: classpath*:/mapper/**/*.xml
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      timeout: 10000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 2
          max-wait: 10000ms
  sql:
    init:
      mode: always

logging:
  level:
    org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerMapping: ERROR

jwt:
  secret: wakapix_secret_key_2024_padding_to_32bytes_minimum
  expiration: 86400000

sms:
  expire-time: 300000

wechat:
  app-id: wx572027b49da6eee2
  app-secret: ef91dd11866eaa0020417e3f283c4a4f
  redirect-uri: http://localhost:8080/api/auth/wechat/callback
